<Window x:Class="Anomaly_Detector.Views.CameraWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Camera Capture" Height="450" Width="800">
    <Grid>
        <GroupBox Margin="50" Background="Gray">
            <Image Name="CameraFeedImage" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </GroupBox>
        <Button Content="Capture" VerticalAlignment="Bottom" HorizontalAlignment="Center" Click="CaptureButton_Click" Margin="10"/>
    </Grid>
</Window>
